# Loader log level
log-level = "info"
# Loader log file
log-file = "loader.log"

# Directory of the dump to import
dir = "./"

# Loader pprof addr
pprof-addr = ":10084"

# We saved checkpoint data to tidb, which schema name is defined here.
checkpoint-schema = "tidb_loader"

# Number of threads restoring concurrently for worker pool. Each worker restore one file at a time, increase this as TiKV nodes increase
pool-size = 16

# An alternative database to restore into
#alternative-db = ""
# Database to restore
#source-db = ""

##we support regular expression , start with '~' to declare using regular expression.
#
#do-db = ["~^b.*","s1"]
#[[do-table]]
#db-name ="test"
#tbl-name = "log"

#[[do-table]]
#db-name ="test"
#tbl-name = "~^a.*"

#ignore-db = ["~^c.*","s2"]
#[[ignore-table]]
#db-name ="test"
#tbl-name = "~^a.*"

# DB config
[to]
host = "127.0.0.1"
user = "root"
password = ""
port = 4000

# our Selector is a two level selector (schemal level, and table level)
# in order to support `renameShardingSchema`, always need this extra rule
# [[route-rules]]
# schema-pattern = "shard_db_*"
# target-schema = "shard_db"

# [[route-rules]]
# schema-pattern = "shard_db_*"
# table-pattern = "shard_table_*"
# target-schema = "shard_db"
# target-table = "shard_table"


