#!/bin/bash
# parameter 1: process name

process=$1

while true
do
    c=$(pgrep $process | xargs ps)
    if [[ $c == "" ]]; then
        echo "wait process $process exit..."
        break
    fi
    echo $c
    sleep 0.2
done
