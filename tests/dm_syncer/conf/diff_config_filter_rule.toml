# diff Configuration.

log-level = "info"

chunk-size = 1000

check-thread-count = 4

sample-percent = 100

use-rowid = false

use-checksum = true

fix-sql-file = "fix.sql"

# tables need to check.
[[check-tables]]
schema = "dm_syncer"
tables = ["dm_syncer_filter_rule"]

[target-db]
host = "127.0.0.1"
port = 4000
user = "test"
password = "123456"
